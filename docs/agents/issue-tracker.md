# Issue tracker

Tasks and acceptance criteria live in GitHub Issues for `Zh0uHX/dsh-skills`.

Use `gh issue create --body-file <file>` and `gh pr create --body-file <file>`. Each PR references its issue with `Closes #N`. Dependencies use GitHub issue dependencies where supported, with a readable `Blocked by: #N` fallback.

PRs as a request surface: no.
