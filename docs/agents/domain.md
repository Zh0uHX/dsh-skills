# Domain documents

Read `CONTEXT.md` before changing behavior. It contains terms, not implementation plans. Read relevant decisions in `docs/adr/`. Add a decision record when a trade-off would otherwise be hard to reconstruct.
